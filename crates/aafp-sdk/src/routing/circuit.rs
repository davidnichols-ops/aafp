//! Circuit breaker state machine and transitions.
//!
//! In this scaffolding, `CircuitState` is defined in [`metrics`] and
//! re-exported here for the module layout described in
//! ADAPTIVE_ROUTING_PLANE.md §12. The full implementation may move
//! the enum into this file or keep it in `metrics.rs`.
//!
//! This is a pre-build scaffolding stub.

pub use crate::routing::metrics::CircuitState;
